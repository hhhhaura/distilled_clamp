# PyTorch: install the wheel matching your CUDA/CPU from https://pytorch.org/ first if needed.
torch>=2.1
tqdm>=4.65
PyYAML>=6.0
mido>=1.3.0
transformers>=4.36.0
tensorboard>=2.14.0
# Optional: used by preprocess.py for some MIDI paths when available
symusic>=0.4.0
pytest>=7.4.0
